# This directory doubles as the Supabase runtime workspace.
# Everything below is runtime data/secrets/generated — never commit.
.env
.env.*
!.env.example
kaipa-client.env
volumes/
_archive/

# Generated by deploy-agent-worker.sh / deploy-rail-query.sh from
# infra/supabase/ sources; the tracked copies live one level up.
/agent-worker.mjs
/agent-worker.compose.yml
/rail-query/
/rail-query.compose.yml

# docker-compose state / local overrides
docker-compose.override.yml
test.http
